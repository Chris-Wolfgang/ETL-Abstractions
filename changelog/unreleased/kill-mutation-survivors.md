type: internal

Mutation testing: the bulk `Increment*(int)` overloads are pinned to record the run start (six new tests) and the seven equivalent mutants (ConfigureAwait flags, a `Task.Yield`, a no-op `finally`) carry documented Stryker exclusions — no survivors left (#452).

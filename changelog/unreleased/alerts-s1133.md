type: internal

S1133 ("remove this deprecated code someday") is excluded per file for `ExtractorBase` / `LoaderBase` / `TransformerBase` in a nested `src/.editorconfig`: their `[Obsolete]` setters are the deliberate markers for the 2026-12-15 removal wave (#484).

type: internal

The three remaining `src` projects set `SuppressTfmSupportBuildWarnings`, matching `Wolfgang.Etl.ErrorPolicies` and every other src project in the fleet. Build-log noise only; no compilation or packaging change.

type: internal

The six options-constructor defaults (`XOptions? options = null`) stay as shipped in 0.25; their removal is scheduled for the 2026-12-15 wave (#636) so this release carries no public-signature change.

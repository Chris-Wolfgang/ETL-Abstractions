type: internal

TestKit doc comments: constructor `<see cref>`s written `Type{T}(…)` so InspectCode resolves them (26 × CS1580; Roslyn accepted both forms) and one Stryker rationale comment reworded so Sonar stops reading it as code (S125).
